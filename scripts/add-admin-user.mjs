#!/usr/bin/env node
/* eslint-disable no-console */
console.error(
  "This script was for the Firebase Emulator and is no longer supported. Use scripts/grant-admin-by-email.mjs against production instead."
);
process.exit(1);
