name: Deploy Functions (Default codebase)

on:
  # Manual deployment only to avoid accidental deploys on push
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'prod'
        type: choice
        options:
          - dev
          - staging
          - prod

concurrency:
  group: deploy-functions-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Firebase
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'prod' }}
    
    # Skip deployment for pull requests
    if: github.event_name != 'pull_request'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: 'yarn'
          cache-dependency-path: 'kateri-monorepo/yarn.lock'

      - name: Enable Corepack
        run: npx -y corepack@latest enable
      
      - name: Pin Yarn 4
        run: corepack prepare yarn@4.1.0 --activate

      - name: Install dependencies
        working-directory: kateri-monorepo
        run: corepack yarn install

      - name: Build functions
        working-directory: kateri-monorepo
        run: corepack yarn workspace @kateri/functions build

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Authenticate (Workload Identity Federation)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT_EMAIL }}
          token_format: access_token
          access_token_scopes: https://www.googleapis.com/auth/cloud-platform

      - name: Set Firebase project alias
        run: |
          case "${{ inputs.environment || 'prod' }}" in
            dev) firebase use dev ;;
            staging) firebase use staging ;;
            prod) firebase use prod ;;
          esac

      - name: Deploy Firestore rules and indexes (keyless)
        run: |
          firebase deploy --only firestore:rules,firestore:indexes

      - name: Deploy Functions (keyless)
        run: |
          firebase deploy --only functions

      - name: Post deployment status
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "✅ Deployment to ${{ inputs.environment || 'prod' }} completed successfully"
          else
            echo "❌ Deployment to ${{ inputs.environment || 'prod' }} failed"
            exit 1
          fi

  notify:
    name: Notify Deployment
    runs-on: ubuntu-latest
    needs: deploy
    if: always()
    steps:
      - name: Notify deployment status
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "🚀 Functions deployed successfully to ${{ inputs.environment || 'prod' }}"
          else
            echo "💥 Function deployment failed"
          fi